import React from 'react';

import * as Styled from './styles';

const Header = () => (
    <Styled.Container>
        <Styled.HeaderBox>
            <Styled.TopHeaderBox>
                <Styled.Title>
                    Agenda de churras
                </Styled.Title>
            </Styled.TopHeaderBox>
        </Styled.HeaderBox>
    </Styled.Container>

);

export default Header;
