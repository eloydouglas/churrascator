import React from 'react';

import * as Styled from './styles';

const Header = () => (
    <Styled.Container>
        <Styled.HeaderBoxContainer/>
        <Styled.HeaderBox>
            <Styled.Title>
                Agenda de Churras
            </Styled.Title>
        </Styled.HeaderBox>
    </Styled.Container>

);

export default Header;
